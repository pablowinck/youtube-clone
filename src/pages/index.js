import { Button } from "@material-ui/core";
import React from "react";

const Home = () => (
    <>
        <div>Clone youtube com Next.js + Material-UI</div>
        <Button variant="outlined" color="secondary">
            Hello World
        </Button>
    </>
);

export default Home;
